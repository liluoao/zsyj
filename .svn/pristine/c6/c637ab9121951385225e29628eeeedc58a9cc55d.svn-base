//打开添加页面
function openadd(){
	$.dialog({id:'recruitment_add'}).close();
	$.dialog.open(addUrl, {
		title: '添加新的职位',
		width: 700,
		height:250,
		lock: true,
		border: false,
		id: 'recruitment_add',
		drag:true
	});
}
